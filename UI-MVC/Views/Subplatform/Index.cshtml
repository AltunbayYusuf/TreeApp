@model IntegratieProject.BL.Domain.project.SubPlatform

@{
    ViewBag.Title = Model?.CompanyName ?? "Subplatform";
    Layout = "_AdminLayout";
}

<div class="admin-page">
    <div class="admin-header">
        <div>
            <h1 class="admin-title">@(Model?.CompanyName ?? "Subplatform")</h1>
            <p class="admin-subtitle">Overzicht van dit subplatform</p>
        </div>
    </div>
</div>
